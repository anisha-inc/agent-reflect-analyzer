Тебе дан кластер summaries сессий Claude Code, объединённых k-means.
Дай ему короткое имя и описание pattern'а, который их объединяет.

ВЫХОД — STRICT JSON:
{
  "name": "Короткое имя кластера (≤60 символов, без emoji)",
  "description": "2-3 sentences по-русски — что общего в этих сессиях и какой это pattern."
}

Отвечай ТОЛЬКО валидным JSON, без markdown.
